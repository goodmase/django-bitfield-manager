============
Installation
============

At the command line::

    $ easy_install django-bitfield-manager

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-bitfield-manager
    $ pip install django-bitfield-manager
